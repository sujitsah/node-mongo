{{#each ideas}}
<div class="card card-body">
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <a href="ideas/edit/{{id}}" class="btn btn-dark btn-block">edit</a>
</div>
{{else}}
<p>no idea</p>

{{/each}}